
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark"> 
<%= link_to("Luga Pasa", root_path , class: "navbar-brand") %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav w-100">
            <li class="nav-item active dropdown ml-auto">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

                  <%= signed_in? ? current_user.full_name : "Admin Login" %>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                  <% if signed_in? %>
                    <%= link_to "Add Cloth", new_dress_path, class:"dropdown-item"  %>

                    <%= link_to "Edit Profile", edit_user_path(current_user), class:"dropdown-item" %> 
                    <%= link_to "Sign Out",logout_path, method: :delete, class:"dropdown-item"%>

                  <%else %>
                    <% if User.count == 0%>
                      <%= link_to "Sign Up",signup_path, class:"dropdown-item" %>
                    <%end%>
                    <%= link_to "Login",login_path, class:"dropdown-item" %>
                  <%end%>
                </div>
            </li>
        </ul>
    </div>
</nav>