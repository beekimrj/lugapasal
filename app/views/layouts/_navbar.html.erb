<div class="navigation-bar" id="navigation-bar">
    <%= link_to("Luga Pasa", root_path , class: "company-name") %>
    <%  if signed_in? %>
      <%= link_to current_user.full_name, "#" %> 
      <%= link_to "Add Cloth", new_dress_path, class:"dropdown-item"  %>

      <%= link_to "Edit Profile", edit_user_path(current_user), class:"dropdown-item" %> 
      <%= link_to "Sign Out",logout_path, method: :delete, class:"dropdown-item"%>
  <%end%>
    <div class="search-container">
      <%= form_with url: search_path, method: :get, local: true do |f| %>
        <%= f.text_field :search , value: @searched_item, placeholder: "Search.."%>
        <%= f.submit "Search", class: "nav-search"  %>
      <% end %>
    </div>
  </div>